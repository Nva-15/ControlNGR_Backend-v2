# =====================================================
# Configuracion de PRODUCCION - Control NGR
# =====================================================

# Puerto del servidor
server.port=8080
# Escuchar en todas las interfaces de red (permite acceso desde otras PCs en la red)
server.address=0.0.0.0

# =====================================================
# Base de datos MySQL
# =====================================================
spring.datasource.url=jdbc:mysql://localhost:3306/controlngr?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =====================================================
# Hibernate/JPA - PRODUCCION
# =====================================================
# IMPORTANTE: En produccion usar 'validate' o 'none'
# NO usar 'update' en produccion para evitar cambios no deseados
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# =====================================================
# Almacenamiento de Imagenes - RUTA RELATIVA
# =====================================================
# Usa ./img/ (carpeta img en el mismo directorio del JAR)
app.storage.location=file:./img/

# =====================================================
# Multipart para subida de archivos
# =====================================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# =====================================================
# Logging - Produccion (menos verbose)
# =====================================================
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.org.apache.catalina=OFF
logging.level.com.example.ControlNGR=INFO

# =====================================================
# JWT
# =====================================================
jwt.secret=tu_clave_secreta_jwt_para_controlngr_2025_segura_muy_larga
jwt.expiration=36000

# =====================================================
# Email (OPCIONAL - configurar si usas en produccion)
# =====================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=proyecto.24web@gmail.com
spring.mail.password=xchh kxol xjqa rppp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

app.mail.enabled=true
app.mail.from=proyecto.24web@gmail.com
app.mail.from-name=Sistema Control NGR

spring.application.name=ControlNGR

# =====================================================
# Configuracion de Email (SMTP Gmail)
# =====================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=proyecto.24web@gmail.com
spring.mail.password=xchh kxol xjqa rppp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Configuracion adicional de notificaciones
app.mail.enabled=true
app.mail.from=proyecto.24web@gmail.com
app.mail.from-name=Sistema Control NGR

# Configuracion del servidor
server.port=8080

# Configuracion de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/controlngr?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Desactivar validaciones estrictas
spring.security.filter.dispatcher-types=REQUEST,ERROR,ASYNC,FORWARD,INCLUDE

# Configuracion de recursos estoticos
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

# Permitir todos los motodos HTTP
spring.mvc.dispatch-options-request=true
spring.mvc.dispatch-trace-request=true

# Logging para debug
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.apache.catalina=OFF


# Multipart para subida de archivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Ruta base para almacenamiento de imagenes (Local)
# El prefijo file:/// indica que es una ruta del sistema de archivos
app.storage.location=file:///D:/ControlNGR/img/

# Zona horaria de Jackson (serialización JSON)
spring.jackson.time-zone=America/Lima

# Configuracion de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Configuracion de logging
logging.level.com.example.ControlNGR=DEBUG

# Configuracion de JWT
jwt.secret=tu_clave_secreta_jwt_para_controlngr_2025_segura_muy_larga
# Expiracion del token en segundos (10 horas)
jwt.expiration=36000


